---
- hosts: localhost
  connection: local
  become: true

  roles:
    # NOTE: Tired of relying on outside roles. CI was constantly breaking due to
    # external dependencies. Keeping a minimal depedency by assuming that Java
    # was previously installed on the container image used for CI. Tests are no
    # longer relying on external resources, making them more predictable,
    # consistent, and easily reproducible.
    # - ansiblebit.oracle-java

    - macunha1.confluent_kafka
